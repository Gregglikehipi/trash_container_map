services:
  react-app:
    container_name: react-app
    build:
      context: ./front/frontend
      dockerfile: Dockerfile.react
    ports:
      - "3000:80"
    networks:
      - app-network

networks:
  app-network:
    driver: bridge